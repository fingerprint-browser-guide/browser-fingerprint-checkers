# 研究与测试方法

最后更新：2026-07-22

## 项目目标

本项目比较的是浏览器指纹检测工具的测试目标、公开字段、结果含义、方法透明度和隐私边界，不比较“绕过率”“账号存活率”或所谓全网通过率。

## 工具纳入标准

首版工具至少满足一项：

1. 提供可直接访问的浏览器指纹、隐私泄漏或自动化检测页面；
2. 是具有公开代码或公开研究方法的代表性项目；
3. 在中文浏览器指纹、隐私或多环境测试语境中具有较高认知度；
4. 能补充其他工具未覆盖的测试目标。

工具数量不代表穷尽所有服务。未纳入不等于质量较差。

## 证据等级

| 等级 | 定义 | 可以支持的结论 |
|---|---|---|
| A | 官方源代码、研究论文或公开方法与字段同时存在 | 可以描述测试目的、主要组件和方法边界；仍不自动验证线上部署与源码完全一致 |
| B | 官方测试页、字段说明和隐私政策较完整，但算法或源代码未充分公开 | 可以描述页面公开能力，不能复原综合算法或误报率 |
| C | 官方页面展示字段或总分，但权重、样本、验证集或误报率有限公开 | 只能描述其公开声称检查什么，不能把分数当作跨站标准 |

等级评价的是**可复核性**，不是检测质量排名。

## 来源优先级

1. 工具官网的测试页、文档、隐私政策；
2. 官方 GitHub 代码仓库；
3. 维护组织或研究机构的论文与项目页；
4. 标准组织文档只用于解释通用技术；
5. 第三方评测不用于确认产品功能或分数有效性。

## 冲突处理

- 官网营销页与隐私政策冲突时，正文同时保留两种口径，并建议按更保守的隐私政策理解。
- 开源仓库与线上服务可能不是同一版本时，只确认仓库公开性，不声称逐行对应。
- 同一品牌的开源客户端与闭源商业服务分开描述。
- 页面没有公开权重、样本或误报率时，明确标记“有限公开”，不猜测算法。

## 首版为什么没有真实横向测试

统一测试至少需要固定硬件、操作系统、浏览器版本、网络、代理、时区、语言、屏幕、扩展和执行时间，并保存原始日志。当前首版只进行了公开资料核查，没有使用可控本地设备进行实验，因此所有 `tested` 字段均为 `false`。

## 建议的复现实验

### 环境记录

- `test_id`
- `tested_at_utc`
- `os_name` / `os_version`
- `browser_product` / `browser_version` / `engine_version`
- `device_type`
- `screen_resolution` / `viewport`
- `languages` / `timezone`
- `extensions_and_privacy_settings`
- `network_type` / `proxy_or_vpn`
- `exit_country` / `asn`（公开分享时应脱敏）
- `webrtc_policy`

### 执行顺序

1. 创建干净环境并记录配置。
2. 逐个运行工具，避免同时开多个检测页改变资源或权限状态。
3. 同一工具连续运行三次。
4. 完全退出并重启浏览器，再运行一次。
5. 只改变一个变量，重复测试。
6. 浏览器或内核更新后重复全套测试。

### 结果保存

- 页面 URL 与访问时间；
- 原始文本、JSON 或导出文件；
- 全页截图与关键字段截图；
- Canvas、WebGL、Audio、TLS 等 hash；
- 每次测试的字段差异；
- 工具的页面版本或提交版本线索；
- 对数据保留和第三方请求的观察。

### 判断规则

- 不平均不同工具的总分；
- 不把 `clear`、`normal`、`human` 或 100% 写成全网通过；
- 不用一次测试证明稳定性；
- 不用检测器结果推断真实身份；
- 区分“未检测到”与“确认不存在”；
- 将目标网站的授权测试作为单独层级，不由公共检测器代替。

## 更新策略

事实字段建议每 90 天复核一次；工具重大改版、隐私政策变化或浏览器内核大版本变化时应提前更新。每次变更需要记录来源 URL、访问日期和变更说明。

